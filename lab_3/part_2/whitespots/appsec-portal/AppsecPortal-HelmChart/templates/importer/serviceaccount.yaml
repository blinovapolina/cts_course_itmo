{{- if .Values.importer.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "importer.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "portal.labels" (dict "context" . "component" .Values.importer.name "name" .Values.importer.name) | nindent 4 }}
  {{- with .Values.importer.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
secrets:
  - name: {{ template "portal.fullname" . }}-secret
{{- end }}