{{- if .Values.portal.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "portal.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "portal.labels" (dict "context" . "component" .Values.portal.name "name" .Values.portal.name) | nindent 4 }}
  {{- with .Values.portal.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
secrets:
  - name: {{ template "portal.fullname" . }}-secret
{{- end }}