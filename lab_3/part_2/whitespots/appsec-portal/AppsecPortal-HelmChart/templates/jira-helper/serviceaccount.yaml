{{- if .Values.jirahelper.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "jirahelper.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "portal.labels" (dict "context" . "component" .Values.jirahelper.name "name" .Values.jirahelper.name) | nindent 4 }}
  {{- with .Values.jirahelper.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
secrets:
  - name: {{ template "portal.fullname" . }}-secret
{{- end }}